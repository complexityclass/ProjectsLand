# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

#APIs
GET     /comments                   controllers.Application.comments
POST    /comment                    controllers.Application.comment(author, text)
POST    /auth                       controllers.DashboardController.auth(login, pass)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
GET     /webjars/*file              controllers.WebJarAssets.at(file)
GET     /comments                   controllers.Application.comments
GET     /react                      controllers.Application.react
GET     /project_info               controllers.Application.projectInfo

GET     /dashboard                  controllers.DashboardController.dashboard